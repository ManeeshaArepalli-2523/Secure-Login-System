# Secure-Login-System
Develop a secure login web app  Key Features: User registration and login using hashed passwords (bcrypt or Argon2) Basic input validation and protection from SQL injection Session management with logout feature Optional: Two-Factor Authentication (2FA) Expected Outcome: A secure login system with hashed passwords, input validation, session managem
